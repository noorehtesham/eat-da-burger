<h1> Eat da burger!</h1>

<h2> burgers you have eaten: </h2>

<ul>
    {{#each burgers }}
    {{#unless devoured}} 
        {{> burgers/burger-block devoured = true }}
    {{/unless}}
    {{/each}}
</ul>

<h2> Burgers that have yet to be eaten: </h2>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured = false}}
    {{/if}}
  {{/each}}
</ul>



<h2> Add a burger</h2>
<form class="create-form">
    <div class="form-group">
        <label for="bg">Add a burger </label>
        <input type="text" id="bg" name="name">
    </div>

    <div class="form-group">
        <label for="burger">Devoured</label><br>
        <input type="radio" name="devoured" value="1" checked> Devour it!<br>
        <input type="radio" name="devoured" value="0"> Don't devour
    </div>
  
    <button type="submit">Add Burger</button>
</form>